import { createTheme } from "@mui/material";

const theme = createTheme({
  typography: {
    modal: {
      paddingBottom: "0.7em",
      fontWeight: "500",
      textAligh: "center",
    },
  },
});

export { theme };
